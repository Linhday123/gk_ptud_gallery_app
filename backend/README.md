# Photo Manager Backend (FastAPI)

## Run

```powershell
cd d:\HocTap\2026\PTUD\GK\backend
pip install -r requirements.txt
uvicorn main:app --reload
```

Backend: `http://localhost:8000`

Uploads are served at: `http://localhost:8000/uploads/<filename>`

